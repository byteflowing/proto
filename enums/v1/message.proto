syntax = "proto3";

package enums.v1;

enum MailVendor {
  MAIL_VENDOR_UNSPECIFIED = 0;
  MAIL_VENDOR_SELF = 1; // 自有服务
  MAIL_VENDOR_ALIYUN = 2; // 阿里云
  MAIL_VENDOR_TENCENT = 3; // 腾讯云
  MAIL_VENDOR_HUAWEI = 4; // 华为云
  MAIL_VENDOR_VOLC = 5; // 火山云
}

enum MailContentType {
  MAIL_CONTENT_TYPE_UNSPECIFIED = 0;
  MAIL_CONTENT_TYPE_TEXT = 1; // 纯文本
  MAIL_CONTENT_TYPE_HTML = 2; // html格式
}

enum SmsVendor {
  SMS_VENDOR_UNSPECIFIED = 0;
  SMS_VENDOR_ALIYUN = 1; // 阿里云
  SMS_VENDOR_TENCENT = 2; // 腾讯云
  SMS_VENDOR_HUAWEI = 3; // 华为云
  SMS_VENDOR_VOLC = 4; // 火山云
}

enum MessageSenderType {
  MESSAGE_SENDER_TYPE_UNSPECIFIED = 0;
  MESSAGE_SENDER_TYPE_SMS = 1; // 短信发送程序
  MESSAGE_SENDER_TYPE_MAIL = 2; // 邮件发送程序
}

enum Verified {
  VERIFIED_UNSPECIFIED = 0;
  VERIFIED_UNVERIFIED = 1;
  VERIFIED_VERIFIED = 2;
}