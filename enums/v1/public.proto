syntax = "proto3";

package enums.v1;

enum Verified {
  VERIFIED_UNSPECIFIED = 0;
  VERIFIED_UNVERIFIED = 1;
  VERIFIED_VERIFIED = 2;
}

enum Gender {
  GENDER_UNSPECIFIED = 0;
  GENDER_MALE = 1; // 男性
  GENDER_FEMALE = 2; // 女性
  GENDER_OTHER = 3; // 其他
}
