syntax = "proto3";

package wechat.v1;

message WechatCredential {
  // appid
  string appid = 1;
  // secret
  string secret = 2;
}

message WechatConfig {
  repeated WechatCredential credentials = 1;
}